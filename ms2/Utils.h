#pragma once
#include <string>
#include <cmath>
#include <iostream>

using namespace std;
namespace seneca {
	int getInt();
	int getIntMain();
	int getInt(int min, int max);
	bool isInt(string s);
	bool isEmpty(const string s);
}